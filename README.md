# WRB-TV Universal

Web/PWA frontend for WRB-TV. Provider code resolver is prepared for authorized IPTV/Xtream accounts.

## Local
npm install
npm run dev

## Vercel
Import this repository and use the included `vercel.json`. Build: `npm run build`. Output: `dist`.
